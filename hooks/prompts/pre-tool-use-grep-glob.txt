For structural code questions—symbol definitions, callers/callees, dependencies, dependents, impact analysis, architecture overview—use code_intel (mcp_constellation_code_intel) instead of Grep/Glob. Use Grep/Glob for literal text search. If you are searching for a symbol, caller, dependency, or any structural context or relationships, use \`code_intel\` instead — it resolves cross-file relationships and details that text search cannot.
